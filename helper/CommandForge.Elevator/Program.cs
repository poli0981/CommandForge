// CommandForge.Elevator — privileged broker helper.
//
// Phase 0 placeholder. In Phase 3 this process hosts a named-pipe server that runs
// vetted, admin-level catalog commands on behalf of the un-elevated UI (one UAC prompt
// per session). Golden rule: it only ever runs commands from the embedded catalog *by
// id*; it never builds arbitrary command lines from pipe input.

Console.WriteLine("CommandForge.Elevator — broker helper (not yet implemented).");
return 0;
