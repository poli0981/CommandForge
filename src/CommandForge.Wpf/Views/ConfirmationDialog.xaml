<Window x:Class="CommandForge.Wpf.Views.ConfirmationDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:res="clr-namespace:CommandForge.Wpf.Resources"
        Title="{Binding Title}"
        Width="560" SizeToContent="Height" ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner" ShowInTaskbar="False"
        FontFamily="{DynamicResource MaterialDesignFont}"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontSize="{DynamicResource AppFontSize}"
        Background="{DynamicResource MaterialDesignPaper}">
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis" />
    </Window.Resources>
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <DockPanel Grid.Row="0" Margin="0,0,0,12">
            <materialDesign:PackIcon DockPanel.Dock="Left" Kind="Alert" Width="26" Height="26"
                                     VerticalAlignment="Top" Margin="0,2,10,0" />
            <StackPanel>
                <TextBlock Text="{Binding Title}" FontSize="18" FontWeight="Bold" TextWrapping="Wrap" />
                <TextBlock Text="{Binding DangerText}" Opacity="0.7" FontSize="12" />
            </StackPanel>
        </DockPanel>

        <Border Grid.Row="1" Background="{DynamicResource MaterialDesignToolBarBackground}"
                CornerRadius="4" Padding="10" Margin="0,0,0,12">
            <TextBox Text="{Binding CommandLine, Mode=OneWay}" IsReadOnly="True" BorderThickness="0"
                     Background="Transparent" TextWrapping="Wrap" FontFamily="Cascadia Mono, Consolas, Courier New" />
        </Border>

        <TextBlock Grid.Row="2" Text="{Binding Description}" TextWrapping="Wrap" Opacity="0.85" Margin="0,0,0,12" />

        <StackPanel Grid.Row="3" Margin="0,0,0,12"
                    Visibility="{Binding IsDangerous, Converter={StaticResource BoolToVis}}">
            <TextBlock Text="{res:Loc Confirm_TypeToConfirm}" Margin="0,0,0,6" />
            <TextBox Text="{Binding ConfirmationInput, UpdateSourceTrigger=PropertyChanged}"
                     materialDesign:HintAssist.Hint="{Binding Title}" />
        </StackPanel>

        <CheckBox Grid.Row="4" IsChecked="{Binding CreateRestorePoint}"
                  Content="{res:Loc Confirm_CreateRestorePoint}" Margin="0,0,0,16" />

        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="{res:Loc Console_Cancel}" Command="{Binding CancelCommand}"
                    Style="{StaticResource MaterialDesignOutlinedButton}" Margin="0,0,8,0" />
            <Button Content="{res:Loc Detail_Run}" Command="{Binding RunCommand}"
                    IsEnabled="{Binding CanRun}" Style="{StaticResource MaterialDesignRaisedButton}" />
        </StackPanel>
    </Grid>
</Window>
