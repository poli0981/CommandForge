<Window x:Class="CommandForge.Wpf.Views.LegalGateWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:res="clr-namespace:CommandForge.Wpf.Resources"
        xmlns:views="clr-namespace:CommandForge.Wpf.Views"
        Title="{res:Loc LegalGateTitle}"
        Width="700" Height="560"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        FontFamily="{DynamicResource MaterialDesignFont}"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontSize="{DynamicResource AppFontSize}"
        Background="{DynamicResource MaterialDesignPaper}">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="{res:Loc LegalGateTitle}"
                   FontSize="24" FontWeight="Bold" Margin="0,0,0,8" />

        <TextBlock Grid.Row="1"
                   Text="{res:Loc LegalGateIntro}"
                   TextWrapping="Wrap" Opacity="0.8" Margin="0,0,0,16" />

        <views:LegalDocumentsView Grid.Row="2" />

        <CheckBox Grid.Row="3" Margin="0,16,0,0"
                  IsChecked="{Binding IsAgreed}"
                  Content="{res:Loc LegalGateAgree}" />

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button Content="{res:Loc LegalGateExit}"
                    Command="{Binding ExitCommand}"
                    Style="{StaticResource MaterialDesignOutlinedButton}"
                    Margin="0,0,8,0" />
            <Button Content="{res:Loc LegalGateContinue}"
                    Command="{Binding AcceptCommand}"
                    Style="{StaticResource MaterialDesignRaisedButton}" />
        </StackPanel>
    </Grid>
</Window>
