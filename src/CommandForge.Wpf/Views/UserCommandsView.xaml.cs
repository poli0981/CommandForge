using System.Windows.Controls;

namespace CommandForge.Wpf.Views;

/// <summary>The user-defined commands view (non-elevated, separate from the catalog).</summary>
public partial class UserCommandsView : UserControl
{
    public UserCommandsView() => InitializeComponent();
}
